<% form_for([@band, @song]) do |f| %>
  <%= f.error_messages %>
  <p>
		<%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
		<%= f.label :lyrics %><br />
    <%= f.text_area :lyrics %>
  </p>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>