<h1>Listing attendances</h1>

<table>
  <tr>
    <th>User</th>
    <th>Show</th>
  </tr>

<% for attendance in @attendances %>
  <tr>
    <td><%=h attendance.user_id %></td>
    <td><%=h attendance.show_id %></td>
    <td><%= link_to 'Show', object_path(attendance) %></td>
    <td><%= link_to 'Edit', edit_object_path(attendance) %></td>
    <td><%= link_to 'Destroy', object_path(attendance), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New attendance', new_object_path %>
